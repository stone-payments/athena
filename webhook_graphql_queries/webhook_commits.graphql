query ($repo: String!, $branch: String!, $number_of_repos: Int!, $since: GitTimestamp!, $until: GitTimestamp!, $org: String!) {
  repository(owner: $org, name: $repo) {
    repositoryId: id
    repo_name: name
    ref(qualifiedName: $branch) {
      branchName: name
      target {
        ... on Commit {
          history(first: $number_of_repos, since:$since, until:$until) {
            pageInfo {
              endCursor
              hasNextPage
            }
            edges {cursor
              node {
                commitId: id
                messageHeadline
                oid
                committedDate
                additions
                pushedDate
                deletions
                changedFiles
                author {
                  user {
                    devId: id
                    login
                  }
                }
              }
            }
          }
        }
      }
    }
  }
  rateLimit {
    remaining
    resetAt
  }
}
